**Added:**

* Added doctest for `--pages` option for paginate cli command.

**Fixed:**

* Fixed handling of `--pages` for paginate cli command.

**Changed:**

* Changed `--pages` option page indices for paginate cli command to start at 1 instead of 0.