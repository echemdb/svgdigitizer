**Added:**

* Added DOI and citation handling to `paginate` cli command enabling the automatic renaming with echemdb identifier.
* Added `get-doi` cli command to extract the DOI from the first page of the PDF file and print it.
* Added `get-citiation` cli command to print the bibtex citation downloaded .
* Added `rename-by-key` cli command to rename the pdf file using the identifier build from the citation.
* Added requests to dependencies.
