**Added:**

* Added `--citation-key` to parse the name of a bibliography entry in the bibliography file parsed with `--bibliography`.

**Changed:**

* Changed `--bibliography` from type bolean to path, containing BibTex entries.
